{
    "scripts": {
        "tests": "bash ./vendor/bin/phpunit --configuration phpunit.xml",
        "testsdox": "bash ./vendor/bin/phpunit --testdox --configuration phpunit.xml"
    },
    "autoload": {
        "classmap": [
            "src/"
        ]
    },
    "require": {
        "php": ">=7.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^8"
    }
}
